const rules = require("../data/rules.json")

const items = rules
  .map(
    (rule) =>
      `      <li><tt style="white-space: pre;">${rule.id.toLowerCase()}</tt></li>`,
  )
  .join("\n")

console.log(items)
